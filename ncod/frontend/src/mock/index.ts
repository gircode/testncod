import { mockAuth, mockDevice, mockSlave } from '@/mock/api'
import type { User, LoginResponse, ListResponse, Device, Slave } from '@/mock/types'

export {
  mockAuth,
  mockDevice,
  mockSlave
}

export type {
  User,
  LoginResponse,
  ListResponse,
  Device,
  Slave
} 